# Verify the port (usually 3306) and the database name (e.g., your_database_name)
spring.datasource.url=jdbc:mysql://localhost:3306/oil_shop
server.port=8081
# Verify your MySQL username and password
spring.datasource.username=root
spring.datasource.password=1234

# Keep these standard JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true